replicaCount: 2

image:
  repository: ghcr.io/swethaptech-lgtm/devops-demo
  tag: "v1"
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 5000
  targetPort: 5000
  nodePort: 30081

containerPort: 5000

readinessProbe:
  path: /health
  port: 5000

livenessProbe:
  path: /health
  port: 5000
