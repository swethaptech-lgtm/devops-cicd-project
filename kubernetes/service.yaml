apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: devops-demo
spec:
  type: NodePort

  selector:
    app: nginx-demo

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
